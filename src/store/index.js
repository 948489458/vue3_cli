import { createStore } from 'vuex'
import  globalConfig from './modules/globalConfig'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    globalConfig
  }
})
